# Puerto del servidor: Railway usa ${PORT}, localmente usa 8080
server.port=${PORT:8080}

# Configuración de la base de datos (Railway inyectará DATABASE_URL)
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/tokenbd?useSSL=false&serverTimezone=America/Lima}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASS:123456}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# JWT
jwt.secret=${JWT_SECRET:cbdjksAHJHDJas7832hjsad8923hsad9823jsad9asdhj1j23h1j23}

# Subida de archivos
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# JSON
spring.jackson.serialization.write-dates-as-timestamps=false

# Logging
logging.level.root=debug
